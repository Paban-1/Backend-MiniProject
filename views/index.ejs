<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Document</title>
  </head>
  <body>
    <div class="main w-full min-h-screen bg-zinc-900">
      <div class="form px-10 py-10 text-white">
        <form action="create" method="post">
          <input
            class="block px-5 py-2 bg-zinc-800 rounded-md outline-none w-full"
            type="text"
            placeholder="Title Gose Here"
            name="title"
          />
          <textarea
            class="block px-5 py-2 mt-2 bg-zinc-800 rounded-md w-full outline-none resize-none"
            placeholder="Write your task here "
            name="detiles"
          ></textarea>
          <input
            class="mt-2 bg-blue-600 px-5 py-2 rounded-md"
            type="submit"
            value="Create Task"
          />
        </form>
      </div>
      <div class="task p-10 flex gap-3 flex-wrap">
        

        <% if(files.length > 0){ %>
            <% files.forEach(function(val){ %>
                <div class="task w-72 px-3 py-4 rounded-md bg-zinc-800">
                    <h1 class="text-white text-3xl tracking-tighter">
                        <%= val %>
                    </h1>
                    <a href="" class="text-blue-500 inline-block mt-3">Read More.</a>
                  </div> 
            <% }) %>
        <% } else { %>
            <h3 class="text-zinc-600">no task yet.</h3>
        <% } %>


       
        <!-- <div class="task w-72 hidden px-3 py-4 rounded-md bg-zinc-800">
          <h1 class="text-white text-3xl tracking-tighter">
            Some task goes here.
          </h1>
          <a href="" class="text-blue-500 inline-block mt-3">Read More.</a>
        </div>
        <div class="task w-72 hidden px-3 py-4 rounded-md bg-zinc-800">
          <h1 class="text-white text-3xl tracking-tighter">
            Some task goes here.
          </h1>
          <a href="" class="text-blue-500 inline-block mt-3">Read More.</a>
        </div> -->
      </div>
    </div>
  </body>
</html>
